apiVersion: v1
kind: Service
metadata:
  name: {{ include "kattungar-notify.fullname" . }}
  labels:
    {{- include "kattungar-notify.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kattungar-notify.selectorLabels" . | nindent 4 }}
